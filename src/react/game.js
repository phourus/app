/** @jsx React.DOM */
"use strict";
var React = require('react');

// Leaderboards, Milestones
var Game = React.createClass({
     render: function () {
        return (
            <h1>Game</h1>
        );
     }
});
module.exports = Game;