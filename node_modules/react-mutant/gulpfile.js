var gulp = require('gulp');
var browserify = require('browserify');
var source = require("vinyl-source-stream");

gulp.task('default', function() {
  var b = browserify();
  b.add('./index.js');
  return b.bundle()
    .pipe(source('react-mutant.js'))
    .pipe(gulp.dest('build/'))
});
